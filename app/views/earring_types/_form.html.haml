= form_for @earring_type do |f|
  -if @earring_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@earring_type.errors.count, "error")} prohibited this earring_type from being saved:"
      %ul
        - @earring_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
