= form_for @filler_bead do |f|
  -if @filler_bead.errors.any?
    #error_explanation
      %h2= "#{pluralize(@filler_bead.errors.count, "error")} prohibited this filler_bead from being saved:"
      %ul
        - @filler_bead.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
