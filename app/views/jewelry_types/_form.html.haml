= form_for @jewelry_type do |f|
  -if @jewelry_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@jewelry_type.errors.count, "error")} prohibited this jewelry_type from being saved:"
      %ul
        - @jewelry_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
